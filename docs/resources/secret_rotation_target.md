# mazevault_secret_rotation_target

Structured deployment target in a secret rotation pipeline (K8s, DB password, agent sync, DevOps variable, cloud vault).

## Example Usage

```hcl
# See the [Terraform Registry documentation](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/secret_rotation_target) for a complete example.
```

## Argument Reference

Refer to the [Terraform Registry](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/secret_rotation_target) for the full list of supported arguments and attributes.

## Import

Most `mazevault_secret_rotation_target` resources can be imported by UUID:

```
terraform import mazevault_secret_rotation_target.example <uuid>
```
