# mazevault_rotation_config

Full rotation pipeline configuration for a secret (v2: interval, schedule, post-actions).

## Example Usage

```hcl
# See the [Terraform Registry documentation](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/rotation_config) for a complete example.
```

## Argument Reference

Refer to the [Terraform Registry](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/rotation_config) for the full list of supported arguments and attributes.

## Import

Most `mazevault_rotation_config` resources can be imported by UUID:

```
terraform import mazevault_rotation_config.example <uuid>
```
