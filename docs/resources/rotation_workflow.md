# mazevault_rotation_workflow

Simple rotation schedule for a secret with gateway-targeted post-rotation actions.

## Example Usage

```hcl
# See the [Terraform Registry documentation](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/rotation_workflow) for a complete example.
```

## Argument Reference

Refer to the [Terraform Registry](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/rotation_workflow) for the full list of supported arguments and attributes.

## Import

Most `mazevault_rotation_workflow` resources can be imported by UUID:

```
terraform import mazevault_rotation_workflow.example <uuid>
```
