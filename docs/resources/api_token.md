# mazevault_api_token

Scoped, long-lived API token bound to a service identity or user.

## Example Usage

```hcl
# See the [Terraform Registry documentation](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/api_token) for a complete example.
```

## Argument Reference

Refer to the [Terraform Registry](https://registry.terraform.io/providers/MazeVault/mazevault/latest/docs/resources/api_token) for the full list of supported arguments and attributes.

## Import

Most `mazevault_api_token` resources can be imported by UUID:

```
terraform import mazevault_api_token.example <uuid>
```
